pipeline {
    agent any

    stages {
        stage('terraform init') {
            steps {
                echo 'terraform Init'
            }
        }
        stage('terraform plan') {
            steps {
                echo 'terraform plan'
            }
        }
        stage('terraform apply') {
            steps {
                echo 'terraform apply'
            }
        }
    }
}

